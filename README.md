
# Projet 6 HOT TAKES (Sauces)

**Backend : NodeJS , Express , MONGOOSE** 

    •   Installer les dépendances via « npm install »
    •   Démarrer le serveur via « nodemon server »
    
**Base de données** 

    •    Dans backend créer un ficher .env en ajoutant le nom d'utilisateur (DB_USER), le password (DB_PASS) et l'hôte (DB_HOST)
    •   Créer une base de données via MONGOOSE DB;


**Frontend : ** 

    •   Installer les dépendances via « npm install »
    •   Démarrer le serveur via « npm start »
    •   Aller sur la page : http://localhost:4200/
    


**Compte utilisateur**: test@test.fr  Password = 123456




